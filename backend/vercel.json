{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "ignoreCommand": "bash -c 'if git diff --quiet HEAD^ HEAD -- backend/ backend/vercel.json; then echo \"No changes in backend - skipping build\"; exit 1; else echo \"Changes detected in backend - proceeding with build\"; exit 0; fi'"
}
